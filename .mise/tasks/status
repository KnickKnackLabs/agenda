#!/usr/bin/env bash
#MISE description="Show Calendar permission status"
#USAGE flag "--json" help="Output JSON"
set -euo pipefail

exec "${SWIFT:-swift}" "$MISE_CONFIG_ROOT/lib/agenda.swift" "status" "$@"
